# Data Directory

- `processed/`: Generated season/team/player aggregates and other intermediate files. This folder is git-ignored; regenerate as needed via the pipeline.
- Future subdirectories (e.g., `external/`, `raw/`) can be added if we incorporate additional sources—record usage here.
